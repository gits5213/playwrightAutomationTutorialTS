# Copy to `.env` locally. Never commit `.env`.

BASE_URL=https://gitsuniversity.org

# Set to true only when you intend to click Register with valid data
# on the live practice form (may create a practice account).
RUN_REGISTRATION_SUBMISSION=false
